# 需要两份数据：一份用于dnn一份用于xgboost；
由于xgboost对特征要求过高先准备dnn的数据：

两个模型的初始数据：
    1:观察缺失值状况，选取缺失值较少的，这里用missno进行判断，根据之前做的，排除掉一些数据量合适的特征，
        对于不需要的变量要先drop掉之后再dropna（），以保证数据量更多；
    1:首先将bedrooms  ，eval，
    2：将tradetypeid 分开分成两种模型；
    3：特征的衍生：需要将面积房间面积合并，也就是将lenght*width；

dnn:数据准备：
    还是一个特征一个特征的添加进行测试为宜；
    1：还是需要排除掉一些不能使用的特征，之前做了；‘
    2：需要保留省份城市地址邮编这些变量，日期就不要了，主要式由于只有一个月的数据，开始时间的日期影响不大；
    3：由于dnnregressor自带有特征工程的东西，所以没必要自己进行编码；用它给的进行处理特征
    4：去除一些异常值和离群点，之前做过，拿来用就行；
    5：更改dnn的程序，将数据集分成三份，训练测试和预测，
        但是无法实现训练数据和测试数据的随机性；（可以将数据获取写进函数里面用
        一个循环将训练数据测试数据训练和测试写在一个循环里面）
    6：特征工程里面的东西了，再之前需要对类别型变量里面的类别数要进行统计，这样好确定用不用hash

